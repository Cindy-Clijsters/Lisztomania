{% extends 'base.html.twig' %}

{% block title %}Lisztomania :: {% trans from 'users'%}action.add{% endtrans %}{% endblock %}
{% block pageTitle %}{% trans from 'users'%}action.add{% endtrans %}{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb text-right">
        <li><a href="{{ path('rtAdminDashboard') }}">{% trans %}global.dashboard{% endtrans %}</a></li>
        <li><a href="{{ path('rtAdminUserOverview') }}">{% trans %}global.users{% endtrans %}</a></li>
        <li class="active">{% trans from 'users'%}action.add{% endtrans %}</li>
    </ol>
{% endblock %}

{% block mainContent %}  
    
<div class="animated fadeIn">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">

                    {{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}

                        {{ form_row(form.lastName) }}
                        {{ form_row(form.firstName) }}                           
                        {{ form_row(form.email) }}
                        {{ form_row(form.username) }}
                        {{ form_row(form.role) }}

                        {{ form_widget(form.submit) }}
                        <a href="{{ path('rtAdminUserOverview') }}" class="mt-2 btn btn-sm btn-outline-primary">
                            {% trans %}action.cancel{% endtrans %}
                        </a>

                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
</div>
    
{% endblock %}
